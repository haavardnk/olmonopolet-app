import 'package:flutter/material.dart';

class ReleaseTab extends StatelessWidget {
  const ReleaseTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
